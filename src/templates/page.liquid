<div class="page-banner">
  <center><h1>{{ page.title }}</h1></center>
</div>
<div class="container">
  <div class="rte">
    {{ page.content }}
  </div>
  <style>
    .row div{
      background-color: green;
      border-radius: 10px;
    }
  </style>
</div>
